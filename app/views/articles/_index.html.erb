<!--%= form_tag({controller:"articles",action:"index"}, method: :get) do %>-->
<!--   %= select_tag("search", options_for_select([['---並べ替え選択---', ''], ['最新の投稿', 'newpost'], ['古い投稿', 'oldpost']])) %>-->
<!--   %= submit_tag '並べ替える'  %>-->
<!-- % end %>-->

<div class="row row-cols-1 row-cols-md-3 g-4">
  <% articles.each do |article| %>
  <div class="col">
    <div class="card h-100">
        <div class="bd-placeholder-img card-img-top" >
          <%= link_to article_path(article.id) do %>
           <%= attachment_image_tag article, :image, fallback:"no-image.jpg", class:"card-img-top"; %>
          <% end %>
        </div>
        <div class="card-body">
          <h5 class="card-title">
            <%= link_to(article_path(article.id), class:"text-dark") do %>
              <strong><i class="far fa-flag"></i><span> <%= article.title %></span></strong>
            <% end %>
          </h5>
          <p class="card-text">記事：<%= article.body.truncate(30) %>.</p>
          <span><i class="fas fa-user"></i> By <%=link_to article.user.name %></span>
          <span><i class ="favorite_btn_<%= article.id %>"><%= render "favorites/favorite-btn", article: article %></i></span>
        </div>
        <div class="card-footer">
          <small class="text-muted"><%= article.created_at %></small>
        </div>
    </div>
  </div>
  <% end %>
</div>

<!--%= paginate @page %>-->