<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <!--<= render 'users/info', user: @article.user %>-->
      <h2 class="mt-3">New book</h2>
      <!--%= render 'articles/form', article: Article.new %>-->
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
  		<table class='table'>
  		  <tr>
  		    <!--<td>%= link_to user_path(@article.user) do %>-->
        <!--    %= attachment_image_tag(@article.user, :profile_image_id, :fill, 100, 100, fallback: "no-image-icon.jpg") %><br>-->
        <!--    %= @article.user.name %>-->
        <!--    % end %>-->
        <!--  </td>-->
          <td><%= link_to @article.title, article_path(@article) %></td>
          <td><%= @article.body %></td>
          <!--<td class="favorite-btn">%= render "favorites/favorite-btn", article: @article %></td>-->
          <!--<td>いいね数: %= @article.favorites.count %></td>-->
          <% if @article.user == current_user %>
            <td><%= link_to 'Edit', edit_article_path(@article), class: "btn btn-sm btn-success edit_book_#{@article.id}" %></td>
            <td><%= link_to 'Destroy', article_path(@article), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger destroy_book_#{@article.id}"%></td>
          <% end %>
        </tr>
      </table>
      <!--%= render 'books/comment', book: @book,book_comment: @book_comment%>-->
    </div>
  </div>
</div>